## Rules System

This project uses rule-based file sorting.

### ExtensionRule
Matches files by extension (.txt, .pdf, etc.)

### RegexRule
Uses regular expressions to match filenames.

Example patterns:
- `202[0-6]` → matches years 2020–2026
- `report|invoice` → matches report or invoice files